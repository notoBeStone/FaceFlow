//
//  this file is automatically generated, DO NOT MODIFY IT
//
//  Copyright © 2015 Glority. All rights reserved.
//

import Foundation
import DGMessageAPI

public enum MessageFrom: Int, APIEnumrable, CaseIterable {
    case unknown = 0

    case user = 1

    case chatBot = 2

    case system = 3

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(Int.self)

        if let enumValue = MessageFrom(rawValue: rawValue) {
            self = enumValue
        } else if let enumValue = MessageFrom.allCases.first(where: { $0.rawValue == 0 }) {
            self = enumValue
        } else {
            let context = DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Cannot initialize MessageFrom from invalid Int value \(rawValue)")
            throw DecodingError.dataCorrupted(context)
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }
}
